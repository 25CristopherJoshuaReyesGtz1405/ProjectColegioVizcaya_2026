import { SplashScreenPipe } from './splash-screen-pipe';

describe('SplashScreenPipe', () => {
  it('create an instance', () => {
    const pipe = new SplashScreenPipe();
    expect(pipe).toBeTruthy();
  });
});
